import classNames from 'classnames/bind';
import styles from './Image.module.scss';
const cx = classNames.bind(styles);

function Image({ src, alt, classNane }, ref) {
    return <img />;
}

export default Image;
