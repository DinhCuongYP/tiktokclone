import VideoItem from './VideoItem';

function SuggestVideo({ data }) {
    const handleTest = () => {
        console.log(data);
    };

    return (
        <div>
            <button>Test2</button>
        </div>
    );
}

export default SuggestVideo;
