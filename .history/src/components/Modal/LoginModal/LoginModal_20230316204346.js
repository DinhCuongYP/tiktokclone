import ModalWrapper from '../ModalWrapper';
function LoginModal({ handleClose }) {
    return (
        <ModalWrapper>
            <h1>Test</h1>
        </ModalWrapper>
    );
}

export default LoginModal;
