import { WrapperPopper } from '~/components/Popper';

function LoginModal() {
    return (
        <WrapperPopper>
            <h1>Aloo</h1>
        </WrapperPopper>
    );
}

export default LoginModal;
