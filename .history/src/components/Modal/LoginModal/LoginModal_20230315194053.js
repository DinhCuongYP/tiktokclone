import ModalWrapper from '../ModalWrapper';

function LoginModal() {
    return (
        <ModalWrapper className={cx('wrapper')}>
            <button></button>
        </ModalWrapper>
    );
}

export default LoginModal;
