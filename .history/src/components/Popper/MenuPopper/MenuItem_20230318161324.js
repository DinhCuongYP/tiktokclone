// import PropTypes from 'prop-types';
// import Button from '~/components/Button';
// import classNames from 'classnames/bind';
// import styles from './MenuPopper.module.scss';
// const cx = classNames.bind(styles);

function MenuItem({ menuInfo, onClick, isSubMenu }) {
    // const { icon, title, href, to, rightIcon, separate } = menuInfo;
    // if (href || to) onClick = null;
    // const classes = cx('menuItem', {
    //     'sub-menuItem': isSubMenu,
    //     separate,
    // });
    return (
        <h1>Aloo</h1>
        // <Button
        //     iconClassname={cx('icon-menuItem')}
        //     textClassname={cx('text-menuItem')}
        //     className={classes}
        //     leftIcon={icon}
        //     rightIcon={rightIcon}
        //     href={href}
        //     to={to}
        //     onClick={onClick}
        // >
        //     {title}
        // </Button>
    );
}

// MenuItem.propTypes = {
//     menuInfo: PropTypes.object.isRequired,
//     onClick: PropTypes.func,
//     isSubMenu: PropTypes.bool,
// };

export default MenuItem;
